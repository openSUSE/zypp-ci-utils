# Local host serving non public repos
DIST_SUSE_DE="10.145.50.100"
# Host workdir, uid and gid.
# Usually the same as the user running the CI
# uid=1000(zci) gid=1000(zci) groups=1000(zci),469(docker)
CI_ROOT_VOLUME=/home/zci/DOCKER_ZCI_WORKSPACE
CI_UID=1000
CI_GID=1000

